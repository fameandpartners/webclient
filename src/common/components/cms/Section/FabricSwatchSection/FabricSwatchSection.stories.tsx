import React from 'react';
import { storiesOf } from '@storybook/react';
import { action } from '@storybook/addon-actions';
import FabricSwatchSection from './FabricSwatchSection';

storiesOf('Components/FabricSwatchSection', module);
    // .add('Swatch List', () => <FabricSwatchSection products={[]} pids={[]} isAddingToCart={false} isErrorAddingToCart={false} addToCart={action('Add to cart')} removeFromCart={action('Remove from cart')} isRemovingFromCart={false} cart={null} />);
